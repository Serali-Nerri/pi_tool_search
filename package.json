{
  "name": "pi-tool-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deferred tool discovery and activation for the pi coding agent.",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "verify": "npm run check && npm test",
    "deploy": "npm run verify && node scripts/deploy.mjs",
    "test:subagents": "tsx scripts/probe-subagents.mts",
    "configure:subagents": "node scripts/configure-subagents.mjs"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    },
    "@earendil-works/pi-tui": {
      "optional": true
    },
    "typebox": {
      "optional": true
    }
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.0",
    "@earendil-works/pi-server": "0.85.0",
    "@earendil-works/pi-tui": "0.85.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.12",
    "typebox": "^1.3.7",
    "typescript": "^5.9.3"
  }
}
